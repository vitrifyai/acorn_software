# ACORN CryoBLOB Plugin

Standalone ACORN add-on that provides a CryoBLOB tab in the ACORN GUI.

Install into the same Python environment as ACORN:

```bash
uv sync --extra cryoblob
```

For standalone plugin development, install this package from its checkout into the
same Python 3.12 environment as ACORN.
